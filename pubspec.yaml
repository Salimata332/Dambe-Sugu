name: dambe_sugu
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  go_router: ^17.0.0
  firebase_auth: ^6.1.2
  lottie: ^2.4.0

  # --- Tes autres dépendances doivent être ici ---
  fl_chart: ^0.55.1
  image_picker: ^0.8.7
  uuid: ^3.0.7
  provider: ^6.0.5
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/bogolan.png
